# Ask for the user name and assigns to name
name = input("What's your name? ")

# Counts the number of characters of the name
print(len(name))

# Other option, same result
print(len(input("What's your name? ")))
